package com.fit.fitnessapp.memory.domain;

public enum MemoryType {
    SEMANTIC, // AI-generated insights and patterns
    EPISODIC, // Specific events or user notes
    FACT      // Static user preferences (e.g., "allergic to nuts", "hates broccoli")
}
