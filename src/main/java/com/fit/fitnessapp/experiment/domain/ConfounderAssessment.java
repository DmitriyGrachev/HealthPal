package com.fit.fitnessapp.experiment.domain;

/** Explicit structured confounder classification; free text is never parsed into one. */
public enum ConfounderAssessment {
    NONE,
    PRESENT_RESOLVED,
    UNRESOLVED
}
