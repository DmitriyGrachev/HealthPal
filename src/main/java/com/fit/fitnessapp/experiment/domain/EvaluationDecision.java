package com.fit.fitnessapp.experiment.domain;

/** The only recommendation and explicit user decision vocabulary in alpha. */
public enum EvaluationDecision {
    KEEP,
    MODIFY,
    DROP,
    INCONCLUSIVE
}
